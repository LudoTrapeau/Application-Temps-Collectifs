<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.g_concept.tempscollectifs">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light">
        <activity
            android:name=".Login"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".InfoBulle"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".UpdateTC"
            android:theme="@style/AppTheme.CustomTheme"
            android:windowSoftInputMode="stateHidden"/>
        <activity
            android:name=".Accueil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CreationTempsColl" />
        <activity
            android:name=".ValidationPresence"
            android:screenOrientation="portrait" />
        <activity android:name=".Infos" />
        <activity android:name=".Historique" />
        <activity android:name=".Preinscription"
            android:windowSoftInputMode="stateHidden"
            />

        <service
            android:name=".Bundle"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".Deconnexion"></activity>
    </application>
</manifest>